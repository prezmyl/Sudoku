#include <vector>
#include "Element.hpp"

using namespace std;
class Region
{
    private:
      //  vector<vector<element>>& region;

    public:
        bool Update(vector<vector<Element>>& region);
};

